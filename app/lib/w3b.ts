"use client"

import { ethers } from "ethers"

// Only admin can create a db
export async function checkUser() {
    try {
        console.log("checking ueser through web3")











    } catch (error) {
        console.log("error checking user", error)
    }
}